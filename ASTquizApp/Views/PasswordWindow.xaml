<Window x:Class="ASTquizApp.Views.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Administrator Password"
        Height="200"
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Administrator Password"
            FontWeight="Bold"
            FontSize="18"/>

        <PasswordBox
            x:Name="PasswordBox"
            Grid.Row="1"
            Margin="0,15,0,0"/>

        <Label Content="Number of Questions:"
            Grid.Row="2"
            Margin="0,10,0,5"/>

        <TextBox x:Name="QuestionCountTextBox"
         Grid.Row="2"
         Width="80"
         Text="100"
         IsEnabled="False"
         HorizontalAlignment="Left" Margin="81,0,0,50" Grid.RowSpan="2"/>

        <StackPanel
            Grid.Row="4"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,20,0,0">

            <Button
                Width="80"
                Margin="5"
                Content="OK"
                Click="OK_Click"
                IsDefault="True"/>

            <Button
                Width="80"
                Margin="5"
                Content="Cancel"
                Click="Cancel_Click"/>

        </StackPanel>

    </Grid>

</Window>